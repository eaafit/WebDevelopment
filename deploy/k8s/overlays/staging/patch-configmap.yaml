apiVersion: v1
kind: ConfigMap
metadata:
  name: notary-portal-config
data:
  NODE_ENV: staging
  LOG_LEVEL: info
  CORS_ORIGIN: https://notary-staging.example.com
  FRONTEND_URL: https://notary-staging.example.com
  ROBOKASSA_TEST_MODE: 'true'
