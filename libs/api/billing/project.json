{
  "name": "billing",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/api/billing/src",
  "projectType": "library",
  "tags": ["scope:backend"],
  "// targets": "to see all targets run: nx show project billing --web",
  "targets": {
    "test": {
      "dependsOn": ["prisma:generate"]
    }
  }
}
