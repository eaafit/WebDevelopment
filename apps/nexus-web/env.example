# Copy to .env next to docker-compose.yml (do not commit .env).
NEXUS_SECRET=replace-with-a-long-random-string
PORT=3000

# Optional: example URLs if you wire the app to compose services (hostnames db / cache).
# DATABASE_URL=postgresql://postgres:postgres@db:5432/nexus
# REDIS_URL=redis://cache:6379
